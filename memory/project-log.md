# project-log.md — Shmuel / Pagmar Website

## Format
Each entry: date | task | current state | next step.
Entries older than 3 milestones with no active relevance → compressed to one summary line.

---

## Log

### 2026-05-05
**Task:** Project scaffolding — created full directory structure and seed files.
**Current state:** Structure in place. CLAUDE.md is the master orchestrator. Memory, skills, and site folders created. Site is empty (no HTML/CSS/JS yet).
**Next step:** Populate skill files with domain knowledge, then begin site development.

### 2026-05-06
**Task:** Built all four SKILL.md files and finalized CLAUDE.md through collaborative session in Claude.
**Current state:** All skills complete and verified. CLAUDE.md finalized. Memory files in place. HTML scaffolding not yet built.
**Next step:** Scaffold index.html and research.html per ux-architecture skill.

### 2026-05-06
**Task:** Scaffolded index.html and research.html with complete section structure, RTL layout, and navigation.
**Current state:** Both HTML files in place with all sections from ux-architecture skill. Nav links between pages. Empty sections ready for content. CSS and JS links in correct load order.
**Next step:** Build base.css with tokens and resets, then begin section content development.

### 2026-05-06
**Task:** Built Specimen Page — site/index.html, site/styles/base.css, site/styles/specimen.css, site/scripts/specimen.js.
**Current state:** base.css has correct design-system tokens (6 colors, 8 weight names, font-size-min, 4 interactive defaults), correct @font-face pointing to shmuel-variable.ttf. index.html has all 9 sections populated: hero (שמואל static display), try-me (range slider + textarea wired to JS), weights (7-step ramp with data-weight attributes), exit link. specimen.css has nav fixed top-right, all section layouts, 7 font-variation-settings selectors for weight ramp. specimen.js has passive scroll listener (--scroll-weight) and try-me slider/textarea logic. All sections render with serif fallback until VF file is dropped into assets/fonts/.
**Next step:** Build research.html with content and research.css.

### 2026-05-12
**Task:** Color system refactor — renamed confusing variables, merged one near-duplicate, replaced direct hex values with variable references across all CSS files.
**Current state:** 10 root color tokens (down from 11). `--color-black`/`--color-white` inverted naming resolved: now `--color-cream`/`--color-ink`/`--color-ink-warm`/`--color-ink-deep`. `--color-black-exit` merged into `--color-ink-deep`. Added `--color-panel-bg`. All direct hex duplicates in weight rows, research section-pair tokens, and try-me swatches now use variables. rgba cream values standardized to match `--color-cream` base. UI grey hierarchy in try-me.css intentionally left as direct hex (distinct interactive states on neutral panel).
**Next step:** Visual browser verification of all sections per the verification checklist.

### 2026-05-11
**Task:** Replaced fixed pill nav with vertical sidebar across all pages.
**Current state:** .sidebar-nav CSS lives in base.css (shared). Old .site-nav rules removed from specimen.css and research.css. index.html and research.html updated with sidebar HTML (nav-active on current page item). try-me.html created as empty page with sidebar only. body gets padding-right: 3vw from base.css to clear the sidebar. All three sidebar items link to index.html, research.html, try-me.html respectively.
**Next step:** Build out try-me.html content.

### 2026-05-13
**Task:** Built try-me.html into a full-featured font testing tool.
**Current state:** Full-screen contenteditable canvas with default Hebrew passage. Floating control panel (320px, bottom-left, collapsible). 9 panel sections: Alignment (RTL/Center/LTR buttons), Weight (dropdown 300–900 + custom + preview letter), Typography (3 sliders: size 12–360px, letter-spacing, line-height), Colors (6 palette swatches + free color pickers for text/bg), Outline (webkit-text-stroke toggle + color + size), Shadow (toggle + X/Y/blur/color), Features (disabled placeholders for liga/ss01/ss02/hist), Custom CSS (debounced raw injection), Reset button. All driven by a central state object. Weight control is selection-aware — shows "mixed" on multi-weight selection.
**Next step:** Visual verification pass; define which features to wire in the Features panel.

### 2026-05-13
**Task:** Built research.html into a complete 5-section editorial page.
**Current state:** 5 sections: הכתב הבינוני (gold-dark bg), מחקר מקורות (gold-light bg, source viewer), סקיצות (ink-warm bg), בדיקות ותיקונים (green-sage bg), מאפיינים (green-olive bg). Nested sticky tab system — 5 staggered tabs (30px each, 20% wide, positioned RTL). Source viewer loads 18 sources from sources.json; hover crossfades between scan and digitized images; metadata panel shows source name, year, writer, place, letters. Exit link leads to try-me.html. Site is now stable across all three pages.
**Next step:** Content refinement — research page copy is placeholder; try-me Features panel wiring TBD.

### 2026-05-13
**Task:** Content update — replaced all body copy with final corrected Hebrew text.
**Current state:** index.html about-body updated with final 3-paragraph About Shmuel text. research.html all five rs-section__bd blocks replaced with final copy: section 01 includes blockquote with Beit-Arié citation. All placeholder text removed. HTML structure intact.
**Next step:** Visual verification pass across all three pages.

### 2026-05-18
**Task:** Color system refactor — replaced entire previous accent system (#FF6B35, #C8922A, #F0C84A, #7B9068, #3A4920) with three named accent colors.
**Current state:** Three-color palette refactor complete across all four CSS files and specimen.js. base.css: --color-main/second/third tokens + five derived opacities; compatibility aliases retained. specimen.css: section-label→amber, hero hint→cyan, sliders→cyan, char grid→teal hover/cyan selected, weight rows 400→teal/700→amber, exit→cyan/teal. research.css: sections 3–4 now deep teal/amber rooms; tabs match; page/rail bg→#0F0E0C; crossfade line→cyan; src-btn active dot→cyan. try-me.css: full dark-mode panel conversion; panel border→cyan glow; all interactive controls→cyan/teal. specimen.js proximity interpolation→teal. grep pass (step 5) confirmed zero remaining old-accent hex values in styles/. Residual light-mode values in try-me.css sub-elements (.tp-feat-name, .tp-feat-code, .tp-css-hint, .tp-weight-slider-out) not covered by step 4 spec — still rendering incorrectly on dark panel.
**Next step:** Visual verification pass across all three pages; optionally fix residual try-me.css light-mode sub-elements.

### 2026-05-25 (v2)
**Task:** Refined Font Specimen Section — UI, print logic, and shape system overhaul.
**Current state:** English title updated to "Shmuel" (capital S only). Size range removed from panel; size is now random 40–450px per print. Shape options: none / circle / square (ריבוע) / rectangle (מלבן לרוחב) / hexagon (משושה). Shape CSS uses aspect-ratio + clip-path (hexagon). Text color always #000000; background color from color picker. Random rotation -20 to +20 deg per stamp. First 2 prints restricted to left 52% of viewport (clear of panel); 3rd+ span full width. Panel and all its children (h3, labels, inputs, select, button) now use var(--font-secondary) throughout. z-index: removed explicit z-index from sp-titles-wrapper (sticky stage z-index:1 < prints-canvas z-index:50 < panel fixed z-index:200).
**Next step:** Visual verification; tune hexagon clip-path if text clips at small sizes.

### 2026-05-25
**Task:** Built new Font Specimen Section on index.html with interactive print mechanic.
**Current state:** New 300vh `.specimen-outer` section inserted after hero (section ②). Contains: sticky 100vh stage with Hebrew title "שמואל" (wght 300→900 L→R on mouse) and English title "shmuel" (wght 900→300 L→R), two weight labels (top and bottom). `.sp-try-panel` slides in from right after 100px scroll into section (position: fixed, visible class via JS). Print button creates absolute-positioned `.printed-item` divs in `.prints-canvas` at current scroll position + random offset; first 3 prints get z-index:3 (below sticky stage). Hero scroll-hint arrow now clickable (scrolls 100vh). All new CSS namespaced with `sp-` prefix to avoid conflicts. Weight labels use `sp-weight-label` to avoid conflict with existing `.weight-label` in weight rows.
**Next step:** Visual verification across all sections; confirm font loads correctly on variable title; optionally fine-tune print positioning and panel width on real viewport.

### 2026-05-18
**Task:** Color system audit and full palette consolidation — 30 CSS variables → 7-color core palette.
**Current state:** base.css :root now defines exactly 7 core colors (--color-main/second/third/black/dark/grey/white) + 5 derived accent opacities + 2 border vars = 14 total. All 30 legacy/semantic variables removed. All four CSS files fully migrated: zero references to old vars confirmed by grep. Old orange accent orphans (rgba(255,107,53,...)) replaced with amber throughout. Dark-mode rendering bugs in try-me.css fixed (tp-feat-name/code/soon/css-hint/weight-slider-out now use white-toned values). specimen.css weight rows: teal (400) and amber (700) rows gained left-edge accent bar (border-inline-start); weight row 900 label/num/hint now cyan-tinted for accent rhythm on the darkest row. Research editorial room bgs (#1A1816, #2D2C29, #0D2A22, #2A1F05) remain hardcoded as intentional atmospheric colors.
**Next step:** Visual browser verification across all three pages.

### 2026-06-06
**Task:** 8-item UI/UX pass across all pages.
**Current state:** (1) index.html body now has `class="page-index"` so scrollbar uses --color-main (pink). (2) research.css theme-toggle redesigned: flat square button, linear-gradient(90deg) vertical split, solid 2px shadow, rotate(45deg) dark / rotate(-45deg) light / rotate(0deg) hover — no pseudo-element crossfade. (3) base.css sidebar nth-child(3) switched from --color-second to --color-fourth (green) for hover and active states. (4+5) try-me page overhauled: canvas defaults to green text (#39FF14) on black bg; panel moved to top: 3vw / left: 3vw; all border-radius → 0 throughout try-me.css and sources-selector in research.css; weight slider always visible (no hidden custom mode), "מותאם אישית" option removed, slider and select stay in sync; outline section hidden (display:none); canvas starts empty with placeholder "הקלידו לבדיקה"; "לפונט" button added at bottom of panel linking to index.html; all --color-main references in try-me.css replaced with --color-fourth. (6) No legacy blue values found in specimen.css — nothing to change. (7) Exit hover colors: specimen.css → --color-third (orange), research.css → --color-fourth (green), try-me.css → --color-main (pink). (8) Sidebar labels updated across all 3 HTML files: "פונט"→"הפונט", "מחקר"→"המחקר", "נסו אותי"→"הטסטר".
**Next step:** Visual verification across all three pages.

### 2026-06-06
**Task:** Try-me page UI/UX/layout refinements — panel cleanup, weight control overhaul, typography color logic.
**Current state:** (1) Minimize button and מחק button removed from panel; panel is permanently open. (2) Panel header updated: title → "נסו אותי..." at explicit font-size: 15px; subtitle "כלי בדיקה" added with color: var(--color-white). (3) Panel border changed to rgba(245,242,237,0.15) matching exit button border. (4) Canvas gets padding-left: calc(25vw + 4vw) to clear sidebar. (5) .tp-label and .tp-unit now use var(--color-grey) — only interactive elements keep var(--color-fourth). (6) Weight section merged into typography section: preset buttons (רגיל/בולד/בלאק at wght 500/700/900) sit above weight range slider; old select dropdown and preview letter removed. (7) JS wired: preset buttons update slider + apply font-variation-settings; slider syncs active preset; selection monitor syncs slider to selection weight.
**Next step:** Visual verification of try-me.html.

### 2026-06-06
**Task:** Try-me targeted UX/UI fixes — header, weight input, reset animation, palette layout.
**Current state:** Header now reads "כלי בדיקה" in white (removed "נסו אותי..." title and tp-subtitle span). Weight row: `<output>` replaced with editable `<input type="number" id="wght-out" class="ctrl-out ctrl-out-input">` with blur/Enter validation (clamps 500–900); reload icon added (`data-target="tp-wght"`). SLIDER_DEFAULTS now includes 'tp-wght' → value 900, onReset calls setActivePreset. Reload spin: replaced classList.add('is-spinning')/setTimeout with cumulative `data-rot` attribute approach (persistent rotation, no class toggle). `.tryme-sidebar` max-width → 300px !important. `.tp-palette-row` → justify-content: space-between; gap: 0. `.ctrl-out-input` CSS rule added (transparent bg, green text, 4ch width, focus underline). `.is-spinning` CSS rule removed.
**Next step:** Visual verification of try-me.html.

### 2026-06-06
**Task:** Section 01 (הכתב הבינוני) full content and layout refactor.
**Current state:** research.html section 01 rebuilt with new structure. Two h3 sub-headings using `rs-section__subhead` class: "אנטומיה של כתב בינוני" and "הטיפוסים של כתב בינוני — השפעות גאוגרפיות". Inline `rs-figure` for תמונה 1 (margin: 50px 100px). Four `rs-ms-type` blocks for ספרדי/אשכנזי/מזרחי/תימני — each a 70/30 flex split (img-col right, info-col left, 20px gap, margin: 25px 100px). Info-col contains: h4.rs-ms-type__name (50px, wght 900), location subtitle, then a CSS grid key-value table (display:contents on rows, auto/1fr columns RTL, vertical divider via border-inline-start). rs-ms-caption styled as SimplerMono, mirrors research-quote cite, text-align right, margin-top 15px. Light-theme overrides added. Image src paths are placeholders — no images exist in assets/images/ for this section yet. Paragraph body text is structural placeholder; actual text from the attached md file should be substituted manually (encoding was unreadable in session).
**Next step:** Add actual section 01 images to assets/images/; substitute correct paragraph and caption text from טקסטים לסקשן הראשון.md.

### 2026-06-06
**Task:** Section 01 structural layout and CSS fixes — header banner, text centering, wide media blocks.
**Current state:** (1) `.rs-section__hd` — removed `width: calc(100% + 3vw)` and `margin-right: -3vw` that were causing the black gap on the left; now `width: 100%`. (2) HTML — all `rs-section__text-flow` divs renamed to `rs-section__bd text-centered-flow`; all `rs-section__wide-block` divs renamed to `rs-section__wide-media-block` (both only existed in section 0). (3) CSS — added `.text-centered-flow` (flex column, align-items:center) and `.text-centered-flow p` (max-width:40em, centered); added `.rs-section__wide-media-block` (padding-block:25px, padding-inline:100px) and reset rules for child `.rs-figure` and `.rs-ms-type`.
**Next step:** Visual verification of section 01 layout in browser.

### 2026-06-07
**Task:** CSS polish pass — !important audit, duplicate selector merge, redundant rule removal, comment cleanup across all four CSS files.
**Current state:** base.css: merged two body{} blocks; removed redundant body.page-research sidebar nav-active override; removed !important from .sidebar-nav width/padding. specimen.css: removed !important from .char-cell hover/selected; replaced redundant physical+logical padding shorthand on .section-sizes with clean padding-block/inline; removed obvious visual-description comments. research.css: removed redundant html/body reset block; removed !important from .sidebar-nav positioning, .rs-section__hd, .text-centered-flow, .rs-section__wide-media-block, .src-meta-val, .research-quote, .research-quote cite, .theme-toggle-btn, theme-toggle pseudo-elements, all light-theme overrides, #rs-section-0 inner padding, caption max-width. try-me.css: removed !important from sidebar overrides, canvas padding, tryme-sidebar, ctrl-group border, ctrl-out-input, exit link hover states, exit word/sub font-size; merged duplicate .tp-slider-row selectors (4-col → 5-col with reload icon column). Only remaining !important: prefers-reduced-motion media queries (3 in base.css, 1 in research.css) — correct standard practice.
**Next step:** Visual verification across all three pages.

### 2026-06-07
**Task:** Cacao Ceremony animation component — edits and integration into index.html.
**Current state:** Component updated: (1) Location block DOM order swapped — `__he` (חוף פלמחים) first → visually LEFT, `__en` (Palmachim Beach) second → visually RIGHT; `direction: ltr` added to `.cacao-ceremony-location` grid container to neutralize RTL page context. (2) All live text font-sizes multiplied ×1.25 in both desktop and mobile media query rules. (3) קקאו span given class `cacao-ceremony-title__cacao`; CSS rule sets `font-weight` and `font-variation-settings` via `--cacao-title-weight` CSS variable (default 500). (4) JS `updateAnimation()` now also calculates `cacaoTitleWeight = Math.round(lerp(500, 900, progress))` and sets `--cacao-title-weight` on the section element. (5) Layout already used `aspect-ratio: 1920/2260` with no sticky — no change needed. Integration: `cacao-ceremony-animation.css` added to index.html `<head>`; `#cacaoCeremonyAnimationMount` div inserted after `.specimen-outer` section; `cacao-ceremony-animation.js` script tag added before `</body>`; fetch/init inline script added after it.
**Next step:** Visual verification of animation in browser.

### 2026-06-07
**Task:** Cacao Ceremony animation — stage height and weight animation fixes.
**Current state:** `.cacao-ceremony-stage` switched from `aspect-ratio: 1920/2260` to `padding-top: 107%; overflow: hidden` — no more distortion of child elements. `.cacao-ceremony-title__cacao` transition changed to `0.05s linear`. JS `updateAnimation()` now sets `--cacao-title-weight` on both the section element AND directly on the `.cacao-ceremony-title__cacao` element to guarantee the CSS variable reaches the target regardless of cascade issues.
**Next step:** Visual verification of height and weight animation in browser.

### 2026-06-07
**Task:** research.html + research.css layout refactor — unified layout system, sticky rail fix, font-size audit.
**Current state:** Two new layout classes replace all ad-hoc wrappers: `.layout-text` (max-width:40em, centered, flex column, padding-top:6vh; text/blockquote children get text-align:right + margin-inline:0 auto) and `.layout-media` (full-bleed, padding-block:25px padding-inline:100px). Applied across all five sections — two `.layout-text` + five `.layout-media` in section 0; one `.layout-text` each in sections 1–4. Old classes `.rs-section__bd`, `.text-centered-flow`, `.rs-section__wide-media-block` removed from HTML (CSS rules remain as dead code). `.top-grey-rail` now has `background-color: var(--color-black)` with `body.light-theme` override to `--color-white`. Z-index hierarchy confirmed: rail z-index:100, tab-bar z-index:300, sections z-index:1. Font-size audit complete — nine properties raised to 16px: sidebar-nav__item, rs-section__eyebrow, src-btn, sources-viewer::after hint, rs-figure__caption, rs-ms-caption, rs-ms-type__location, rs-ms-grid-key, rs-ms-grid-val.
**Next step:** Visual verification of research.html in browser.

### 2026-06-09
**Task:** Light mode black bar fix — left-edge layout gap and body background.
**Current state:** Two fixes applied: (1) base.css — added `body.light-theme, html:has(body.light-theme) { background-color: var(--color-lightgrey); }` so the root layer switches to light in light mode (previously only .rs-section was overridden, leaving body/html black and exposing them through any layout gap). (2) research.css — `.rs-section__inner` changed from `width: var(--rs-rail-w)` (100vw, wider than content area) + `margin-inline-start: 15px; margin-inline-end: 0` to `width: 100%` with no inline margins; `.sources-viewer` `margin-inline-end: -15px` compensation removed (changed to 0) since the inner no longer carries the offsetting margin.
**Next step:** Visual verification of light mode on research.html.

### 2026-06-09
**Task:** Scrollbar and layout overflow fixes — black bar on left, tab misalignment.
**Current state:** (1) base.css — `::-webkit-scrollbar-track` changed from `var(--color-black)` to `transparent`; `::-webkit-scrollbar-thumb` border changed from `3px solid var(--color-black)` to `3px solid transparent` and `background-clip: padding-box` added — scrollbar no longer renders as a visible coloured bar against page sections. (2) research.css — `--rs-rail-w: calc(100vw)` removed from `:root` (unused since last session). `.rs-section__inner` got `padding-inline: 15px` and `box-sizing: border-box` — eliminates 100vw overflow that caused RTL tabs to shift left and collide with sidebar nav.
**Next step:** Visual verification of research.html scrollbar and tab alignment.

### 2026-06-13
**Task:** weights-icons.html (standalone component) — center-column padding tweak and letter-wave title animation.
**Current state:** (1) `.col-hebrew .center-text-item:not(.weights-title)` gets `padding-left: 0.3em`; `.col-english .center-text-item:not(.weights-title)` gets `padding-right: 0.3em` — pulls the weight labels slightly toward the center gap. (2) `.weights-title` now `display: flex` with no animation of its own; `.col-hebrew .weights-title` and `.col-english .weights-title` set `justify-content: flex-start` (English also `direction: ltr`). (3) `@keyframes weight-animation` renamed to `letter-wave-animation`; now applied per-letter via `.weights-title span` (1.6s ease-in-out infinite alternate, `min-width: 0.1em`, `display: inline-block`). (4) Both title divs given ids (`title-he`, `title-en`); inline script splits each into per-character spans on DOMContentLoaded with staggered `animation-delay` (0.15s × index) for a wave effect across "משקלים" / "Weights".
**Next step:** Visual verification of the wave animation in browser; not yet integrated into index.html.

### 2026-06-13
**Task:** Integrated weights-icons.html standalone component into the main site as `.section-floating-weights`.
**Current state:** New `<section class="section-floating-weights">` inserted in index.html immediately after `.specimen-outer#font-specimen` and before `#cacaoCeremonyAnimationMount` — contains `.center-stage` (he/en weight label columns) and all 6 `.weight-icon-wrapper` divs. CSS appended to specimen.css: container is `position: relative; height: 100vh; overflow: hidden; background-color: var(--color-black)` with local `--center-gap`/`--icon-size` vars; all layout classes (`.center-stage`, `.text-column`, `.weights-title`, `.weight-icon-wrapper`, `.svg-icon-mask`, `.icon-X`, `.weights-icon-X` positions, per-icon `.weight-icon-text` overrides) namespaced under `.section-floating-weights`; all `body:has(...)` hover triggers rewritten to `.section-floating-weights:has(...)` so hover scoping doesn't leak to the rest of the page. JS: `initFloatingWeights()` added to specimen.js (runs `createWaveEffect` on the title elements), called from the main `DOMContentLoaded` listener. IMPORTANT FIX: the standalone component used `id="title-he"`/`id="title-en"` for its animated title, which collided with the existing hero specimen title IDs (`index.html` sp-var-title elements, used by `initSpecimenTitles` in specimen.js) — renamed the new section's title IDs to `fw-title-he`/`fw-title-en` in both the HTML and `initFloatingWeights`. Verified via headless Chromium screenshot: section renders within 100vh bounds (no scroll breakage), wave animation creates per-letter spans, and bidirectional hover scaling (label↔icon) works correctly. No new console errors (pre-existing 404s are from unrelated cacao-ceremony/fingers-animation assets).
**Next step:** Visual verification in a real browser; consider whether `.section-floating-weights` needs a section label/nav entry consistent with other sections.

### 2026-06-14
**Task:** Moved Character Grid section and recolored it to the secondary green accent.
**Current state:** `.section-character-grid` relocated in index.html — now sits immediately after `#cacaoCeremonyAnimationMount` and before `.section-try-me` (previously after `#fingersAnimationMount`, before `.section-display`). specimen.css: `.charview-glyph` color → `var(--color-fourth)`; `.char-cell:hover, .char-cell--selected` color → `rgba(57, 255, 20, 0.5)`; `.char-cell--selected` outline color-mix → `var(--color-fourth)`; new rule `.section-character-grid .section-label { color: var(--color-fourth); }`. specimen.js: proximity hover effect on char cells (previously neon pink `rgba(255, 20, 147, ...)`) now uses `rgba(57, 255, 20, ...)` to match.
**Next step:** Visual verification of the relocated, recolored Character Grid section in browser.

### 2026-06-14 (v2)
**Task:** Added Cap Height and x height metric lines to the character-grid inspector panel.
**Current state:** `.charview-stage` metric-line stack reordered top to bottom: Ascender (800, top:3%) → Cap Height (700, top:12%, new — CSS rule for `[data-metric="cap-height"]` already existed unused) → Heb Height (550, top:29.5%, renamed from "x-Height"; `data-metric` changed `x-height`→`heb-height`) → x height (400, top:45%, new `[data-metric="x-height"]` rule) → Baseline (0, top:76%) → Descender (−200, top:95%). Also fixed hyphenated label in specimen.js: HEBREW_GLYPH_DATA[';'].name "פסיק-עליון" → "פסיק עליון". Changes mirrored from `site/` to `docs/` for index.html, styles/specimen.css, scripts/specimen.js (docs/ is a 1:1 GitHub Pages mirror).
**Next step:** Visual verification of the new metric lines/labels in browser; confirm Cap Height (700) and x height (400) values read well against the live glyph.

### 2026-06-14 (v3)
**Task:** Replaced Character Grid section head with a new "Glyphs Set" header (issue #3).
**Current state:** index.html — the `.section-head` div (containing `.section-label` "מפת תווים" and `.section-aside` "72 תווים") inside `.section-character-grid` was replaced with `.glyph-set-header`, a flex row containing two `.glyph-set-title` boxes: `סט תווים` (dir="rtl") and `Glyphs Set` (dir="ltr"). specimen.css — added `.glyph-set-header` (green `--color-fourth` background, flex, space-between) and `.glyph-set-title` (200px black boxes, green `--color-fourth` text, `--font-primary` wght 700, 50px) directly after the `.section-character-grid` rule. Removed `.section-character-grid .section-label` (added last session, now unused) and the shared `.section-aside` rule (was only used by this header; `.section-sizes` still uses `.section-head`/`.section-label` so those remain).
**Next step:** Visual verification of the new green/black "Glyphs Set" header in browser.

### 2026-06-14 (v4)
**Task:** Repository structure cleanup — removed duplicate `site/` source folder, made `docs/` the single source of truth.
**Current state:** `site/` deleted (user holds an external backup). Fixed ~20 hardcoded `/site/...` absolute paths inside `docs/` (cacao-ceremony-animation.html, fingers-animation.html, fingers-animation-live-text.html, components/fingers-animation.html, styles/cacao-ceremony-animation.css, styles/fingers-animation.css, scripts/fingers-animation.js) — these were broken under GitHub Pages (which serves `docs/` as root) and are now relative paths consistent with index.html's convention. CLAUDE.md, memory/decisions.md, and all three skills/*.md files updated to reference `docs/` instead of `site/`, with explicit rules against ever recreating `site/`. `.gitignore` fixed: `.cache/xx_backup/` → `xx_backup/` (was not actually ignoring the backup folder).
**Next step:** Visual verification that cacao-ceremony and fingers-animation pages now load images/fonts/CSS correctly under `docs/`.

### 2026-06-16
**Task:** Replaced placeholder stylistic alternates in Glyphs Set section with real ss01/ss02 numeral pairs.
**Current state:** `ALTERNATE_CHARS` array replaced by `SS01_DIGITS` / `SS02_DIGITS` constants. `buildCharGrid` now renders two sub-groups inside `#char-grid-alternates` — ss01 (all 10 digits with `.001` variants) and ss02 (0,2,3,4,5,6,9 with `.002`/`.005` variants). Each pair shows base digit → arrow → alternate digit; the variant has `font-feature-settings: "ss01" 1` or `"ss02" 1` via CSS classes `.alt-pair__variant--ss01/ss02`. Seven new `_alt2` keys added to `HEBREW_GLYPH_DATA` for ss02 unicode names (zero.002, two.002, three.002, four.002, five.005, six.002, nine.002). HTML label updated to "חלופות סגנוניות"; `char-grid` class removed from the alternates container (now just `char-grid--alternates`) with a flex-column layout override. Inspector mouseover on variant cells resolves to the correct alt2 glyph key.
**Next step:** Visual verification of stylistic alternates in browser; confirm ss01 vs ss02 renders distinct glyph shapes.

### 2026-06-19
**Task:** Inspiration showcase component, slanted caret proxy, try-me canvas default content, and documentation sync.
**Current state:** (1) research.html — `<section class="inspiration-showcase">` added to section 02 (מחקר מקורות), wired to `inspiration-showcase.js` and `inspiration-showcase.css`. Shows a background image grid (`scans_bg-font.jpg`) with a hover overlay and an auto-generated `#inspirationGrid` populated by JS; `data-mode` attribute toggles between `scans` and `fonts` display. Two `<div class="layout-text">` paragraphs added surrounding the showcase for editorial context. (2) try-me.js + specimen.js — `initSlantedCaret()` implemented in both: JS creates `<div class="caret-proxy">` appended to `<body>`, positions it via `window.getSelection().getRangeAt(0).getClientRects()`, sets height to computed `lineHeight`, adds `.is-active` to show it. CSS rotates it 5° (matching font's slant angle), blinks via `@keyframes caret-proxy-blink`. Native caret hidden with `caret-color: transparent`. (3) try-me.html — `has-fake-cursor` class added to `.tryme-canvas`. CSS `::after` pseudo-element renders a blinking `|` before first interaction; removed permanently on first `focus` (`{ once: true }`), at which point `.caret-proxy` takes over. (4) try-me.js — `DEFAULT_TEXT` populated with beverage-name list; initial `fontSize` raised from 120 to 140px. (5) All fingers and cacao animations remain fully hardcoded in index.html — no `fetch()` calls in specimen.js. Only remaining `fetch()` in the codebase: research.js → `assets/data/sources.json`. (6) Committed and pushed: `chore: major UI refinements, global preloader, integrated async components, and dynamic footer`.
**Next step:** Visual verification in browser; merge branch to main when ready for GitHub Pages deploy.

### 2026-06-20 (v2)
**Task:** Glyph Set data architecture refactor — JSON single source of truth for all glyph categories.
**Current state:** `docs/data/glyph-data.json` created as the sole data source for the Glyph Set section. 12 categories, 236 glyph entries. Each glyph carries: `glyphName`, `display`, `label`, `unicode`, `decimal`, `featureTag` (inherited from category/subgroup), `direction`. Categories: hebrew (27), numbers (10), latin (52), punctuation (34), symbols (22), arrows (10), fractions (9), time/dlig (12), PUA/icons (10), alternates subgroups — ss01 (10) + ss02 (7) + ss03 (8) + ss04 (8), liga (3), dlig (14). PUA display chars are U+E000–E009. `specimen.js` rewritten: `HEBREW_LETTERS`/`MARK_CHARS`/etc. flat arrays removed; `HEBREW_GLYPH_DATA` removed; replaced by `glyphData` object + `glyphMap` lookup keyed by `glyphName`. `buildCell()` helper creates each span with `data-glyph-name`, inline `font-feature-settings`, and `dir` attribute. `buildCharGrid()` iterates categories — existing containers (`containerId`) are populated in-place; new categories (symbols, arrows, fractions, time, pua, liga, dlig) are dynamically created as `.char-category` divs appended to `.charview-grid-panel`. Alternates use existing `#char-grid-alternates` container with 4 sub-groups. `initCharInspector()` now reads from `glyphMap` by `glyphName`; inspector's unicode-name field shows `glyphName` instead of the old `unicodeName`. Fetch path updated to `data/glyph-data.json`.
**Next step:** Visual verification of all 12 categories in the Glyph Set section; confirm PUA icons render once font is loaded; confirm dlig time glyphs substitute correctly.

### 2026-06-20
**Task:** Project-wide CSS variable refactoring and cleanup — all 6 CSS files + 2 JS files.
**Current state:** (1) Renamed 4 core color tokens: `--color-main`→`--color-pink`, `--color-second`→`--color-yellow`, `--color-third`→`--color-orange`, `--color-fourth`→`--color-green` — definitions in `base.css :root`, all usages across `base.css`, `specimen.css`, `research.css`, `try-me.css`, `inspiration-showcase.css`. Backup comment block in `base.css` (lines 42–46) left untouched. (2) Removed 11 redundant variable definitions from `:root`: `--color-ink-warm`, `--color-ink-deep`, `--color-pure-black`, `--color-focus`, `--color-muted-warm`, `--color-surface-dark`, `--color-main-dim`, `--color-main-faint`, `--color-second-dim`, `--color-third-dim`, `--color-third-faint`, `--color-fourth-glow`, `--color-border-dark`, `--color-border-light` (some had zero usages; all remaining usages replaced inline). (3) Replaced 28 unique `rgba(...)` values in `try-me.css` with `color-mix()` expressions keyed to `--color-white`, `black`, or `white`. (4) Fixed `--color-surface-dark` usages in char grid → `color-mix(in srgb, var(--color-white) 10%, transparent)`. (5) Character grid proximity effect in `specimen.js` now reads `--color-green` via `getComputedStyle()` at runtime instead of hardcoding `rgba(57, 255, 20, ...)`. (6) `try-me.js` PALETTES and state default updated from `#39FF14` → `#94ff94` (current green token value). (7) `specimen.js` `NEON_COLORS` array updated to use new variable names. `inspiration-showcase.css` also updated. `inspiration-showcase 2.css` (unreferenced stale duplicate) left as-is.
**Next step:** Visual verification across all three pages; update `skills/design-system/SKILL.md` color palette section to reflect new variable names.

### 2026-06-23
**Task:** Research page holistic architectural refactoring — 4-phase pass across research.html, research.css, research.js, hero-webgl.js.
**Current state:** Phase 1 (Grid): `.rs-section__inner--split` converted from flex-column to 9-column CSS Grid (`repeat(9, 1fr)`, `--rs-grid-gap`). Spacing token system added to `:root` (`--space-xs` through `--space-xl`, `--rs-section-pad-top/bottom`). Scoped grid-column rules for `> .rs-section__hd` (1/-1), `> .layout-text` (2/9), `> .layout-media` (1/-1), `> .rs-section__inner--sources` (1/-1), `> .transition-text` (3/8), `> .rs-beinoni-spectrum` (2/9). `rs-section__inner--sources` gets explicit `display: flex; padding-inline: 0` to override base. `.rs-section::before margin-top: -1px` hack removed. Border-causing-gap elements (`sources-viewer`, `sources-meta`, `sources-letters`) changed from `border: 1px solid` to `box-shadow: inset`. Phase 2 (Tabs): `.rs-tab-bar` gets `min-height: 30px`; per-bar bottom line via `:has(.rs-tab-N)` selectors setting `color` + `box-shadow: 0 1px 0 0 currentColor`. Each `.rs-tab-N` defines `--tab-accent`. `.rs-tab` transition includes `height`. `.rs-tab.is-current` applies `background-color: var(--tab-accent); color: var(--color-black); height: 42px`. `initActiveTabState()` added to research.js using IntersectionObserver (threshold 0.05) to toggle `.is-current` on sections entering/leaving viewport. Phase 3 (Narrative): In section 0, Sources Viewer moved before the opening layout-text (now the first content after the header). Two `<p class="transition-text">` bridge sentences added flanking the viewer. New `.layout-text.rs-beinoni-spectrum` block added after the opening text (defines Beinoni as a point on a spectrum). Phase 4 (Hero): `SHOW_TRAILING_ECHOES = false` flag added to hero-webgl.js; echo position lerp loop (lines ~215–231) and echo draw loop (lines ~248–259) wrapped in `if (SHOW_TRAILING_ECHOES)`. Main circle draw unconditional.
**Next step:** Visual browser verification of the research page — grid alignment, tab active states, Sources Viewer as first content, clean single-circle hero.

### 2026-06-23 (v2)
**Task:** Research page — strict 6-column grid system, tab math refactor, breadcrumb active state.
**Current state:** (1) `--rs-page-margin: 4vw` token added; `.page-research` body changed from `padding-right: 2.5vw` → `padding-inline: 4vw`. (2) `.grid-container-6` utility class added (`repeat(6, 1fr)`, `gap: 2vw`). (3) `.rs-section__inner--split` changed from 9-column to 6-column grid with `gap: 2vw`. Grid column assignments updated: `.layout-text` → `1/-1` with `padding-inline: calc(100%/6)` (simulates 4-of-6 column text width); `.layout-media` → `1/-1` full bleed; `.transition-text` → `2/6`; `.rs-beinoni-spectrum` → `1/-1` with same padding. (4) `.layout-text` standalone: `max-width` removed, `padding-inline: calc(100%/6)` for column alignment. (5) Paragraph `max-width: 35em; margin-inline: auto` removed — width now controlled by grid/padding. (6) Tabs: width changed from `20%` → `calc(100%/6)` (1 of 6 columns); margins remapped to `calc(N×100%/6)` — 5 tabs occupy 5 columns, 6th (leftmost in RTL) stays empty. Tab active state: `height: 42px; margin-top: -12px; background-color: var(--color-black); color: var(--tab-accent)` — accent text on black (inverted from filled accent). (7) `initStickyTabsBreadcrumbs` rootMargin tightened to `-30px 0px -80% 0px`. (8) `.carousel-wrapper` margin updated to `margin-inline: calc(-1 * var(--rs-page-margin)); width: calc(100% + 8vw)` for proper full-bleed breakout.
**Next step:** Visual browser verification of 6-column grid alignment, tab breadcrumb behavior, and full-bleed carousel.

### 2026-06-24
**Task:** 6-column grid completion — strict column assignments, tab stagger shift, border cleanup, HTML narrative reorder.
**Current state:** (1) `.rs-section__inner--split` column gap changed from `2vw` → `0` for pixel-perfect column line alignment. (2) Grid column assignments updated to direct positions: `.layout-text` → `grid-column: 3/6` (3 cols, center-right zone); `.layout-media` → `grid-column: 2/-1` (5 cols, col 1 empty); `.rs-beinoni-spectrum` → `grid-column: 3/6`; all `padding-inline` simulation removed from inside-grid elements. (3) Tab stagger shifted +1 column: rs-tab-0 now starts at `calc(1*100%/6)` (was 0), rs-tab-4 now at `calc(5*100%/6)` — col 1 (rightmost) permanently empty, tracking down the page. (4) Tab borders changed to flat `box-shadow: inset 0 0 0 1px var(--color-black)`; 5 `:has(.rs-tab-N)` colored bottom-line rules removed; tab bar `border-bottom: none`, `box-shadow: 0 1px 0 0 var(--color-black)`. (5) Active tab inverted: `background-color: var(--tab-accent); color: var(--color-black)` (filled accent, not accent text on black). Light-theme override updated to match. (6) `.rs-beinoni-spectrum` `border-inline-end` and `padding-inline-end` removed. (7) `.img-wrapper .caption-hover__overlay` fixed: removed `width:135%/left:50%/transform:translateX(-50%)/bottom:-7%` — now contained within image (`width:100%; left:auto; right:0; transform:none; bottom:0`). (8) `.bottom-tabs-rail .rs-tab` gets `box-shadow:none; border:none` to strip accordion rail outlines. (9) `.rs-ms-type` gap changed to 0; img-col `flex: 0 0 60%` (3 of 5 sub-cols); info-col `flex: 0 0 40%; padding-inline-start: 2rem` (2 of 5 sub-cols) — latches to outer grid column lines. (10) HTML Section 01: two synthetic `transition-text` bridge paragraphs deleted; Sources Viewer moved from top (after header) back to its narrative position (after the intro layout-text block). (11) HTML Section 02: `rs-section__inner + rs-section__bd` wrapper pattern converted to plain `layout-text`; `rs-section__inner` wrapper around inspiration-showcase removed.
**Next step:** Visual browser verification of the complete 6-column grid — column 1 empty throughout, tabs in cols 2–6, text in cols 3–5, Sources Viewer in its narrative position.

### 2026-06-24 (v2)
**Task:** 7 deep-dive layout and spacing refinements — light mode bg, caption hover, tab initial state, double br, vertical rhythm, corner intersection.
**Current state:** (1) `--color-lightgrey` updated `#c0c0c0` → `#F0EFEA` (warm off-white paper tone for light mode). (2) `.img-wrapper` changed to `overflow: hidden`; images normalized from 135% bleed to `width/height: 100%, top/left: 0, transform: none` so caption-hover overlay slides correctly from image bottom. Redundant `.img-wrapper .caption-hover__overlay` override block removed. (3) CSS rule `body:not(.is-past-hero) .rs-tab { height: 30px; background-color: black; color: tab-accent }` added — all tabs stay visually docked while hero is in view; existing `updateRailVisibility()` JS manages the class. (4) Removed explicit `<br>` after `.rs-section__subhead-sub` span (was double-breaking because span is `display: block`). (5–6) Paragraph `margin-bottom` raised `0.5em → 1.5em` in both `base.css` and `research.css`. `.layout-media margin-block: 40px → 6rem`. `.carousel-wrapper margin-block: 4rem → 6rem`. `.sources-group margin-bottom: 100px → 0`. `.scroll-container margin-top: -25px → 0`. `.rs-section__subhead` and `.rs-section__subhead-sub` both set to `margin-top: 7rem; margin-bottom: 2rem`. (7) `.rs-tab-bar::after` corner fixed: `right: 0 → calc(-1*(var(--rs-page-margin) - 2.5vw))`, `width: calc(100vw - var(--rs-page-margin)) → calc(100vw - 2.5vw)` — line now meets sidebar left border at exactly 2.5vw from viewport right.
**Next step:** Visual browser verification of all 7 fixes.

### 2026-06-25
**Task:** Tab layout refactor — flat flex structure replacing nested sticky tab bars.
**Current state:** All 6 tabs (rs-tab-00 through rs-tab-4) consolidated into the single outermost `.rs-tab-bar`. The 4 inner nested `.rs-tab-bar` divs (each previously holding one tab) were removed; their `.rs-layer` wrappers remain as section containers. CSS: default tab state is now inverted (black bg, accent text); active tab (`.is-active`) fills with accent and expands a 60px `::before` tail accordion; the old `is-stuck` class and sentinel mechanism removed; `rs-tab-00` visibility now driven by CSS `.page-research.is-past-hero` instead of a JS `is-visible` toggle; bottom-rail tabs get explicit `right: calc(N * 100% / 6)` column positions; tab bar `margin-right: -2px` hack removed. JS: `initStickyTabShrink` simplified to only expose `window._setGlobalActiveColor`; `initStickyTabsBreadcrumbs.setActiveTab` calls it on each section change; `initBottomAccordion` now tracks section positions (not shared parent tab bar) to show/hide bottom clones; init order flipped so `initStickyTabShrink` runs before breadcrumbs.
**Next step:** Visual browser verification of flat 6-tab row, active-tab tail accordion, bottom rail column alignment.

### 2026-06-25 (v2)
**Task:** Tab layout architecture restoration — reverted flat single-bar to per-section nested tab bars.
**Current state:** The flat single-tab-bar refactor (2026-06-25) was reverted. research.html: outermost `.rs-tab-bar` now holds only rs-tab-00 (position:absolute, col 1) + rs-tab-0 (col 2). Each of the 4 inner `.rs-layer` divs gets its own `.rs-tab-bar` with a single section tab. research.css: `margin: 0` on `.rs-tab-bar` changed to `margin-bottom: 0` to restore the critical `margin-top: -30px` absorb. `.rs-tab-0` through `.rs-tab-4` get `margin-right: calc(N * 100%/6)` offsets that push each single-tab into its correct grid column. `.rs-tab-bar .rs-tab.rs-tab-00` gets `position: absolute; right: 0` to stay out of flex flow. `.bottom-tabs-rail .rs-tab` gets `margin-right: 0` to prevent flex margins from compounding with absolute `right` positioning.
**Next step:** Visual browser verification of grid-aligned tabs, accordion tail, bottom rail alignment.

### 2026-06-25 (v3)
**Task:** Global horizontal line cleanup — top-grey-rail patch removal, ::after line restoration via is-stuck.
**Current state:** `.top-grey-rail` stripped of all hack properties (`border-bottom`, `margin-right: -2px`, `padding-right: 2px`, `width: calc(100% + 2px)`) — now a clean background-only block (`width: 100%`, no border). `.rs-tab-bar::after` rebuilt from scratch: uses `left: 0; right: -2px` (not `width`-based) so the line genuinely spans from tab-bar's left edge to 2px past its right edge, fusing with sidebar `border-left`. Default `background-color: transparent` (invisible). Activated via `.rs-tab-bar:has(.rs-tab.is-stuck)::after` with `--global-active-color` — `:has()` is used because JS adds `is-stuck` to the `.rs-tab` element, not the `.rs-tab-bar`; `:has()` bridges that gap in pure CSS. Removed the dead `opacity: 0 !important` suppression rule that was keeping the line invisible. `.sidebar-nav` confirmed clean: `border-left` only, no `border-top`.
**Next step:** Visual browser verification of the global line appearing on scroll, fusing with sidebar border.

### 2026-06-25 (v4)
**Task:** Active tab tail restoration, global line bleed fix, and z-index layering — research.css + research.js.
**Current state:** Root cause identified: `setActiveTab()` was adding `is-stuck` simultaneously with `is-active` on the tab element; the old `.rs-tab.is-active.is-stuck::before { height: 0 }` rule immediately collapsed the tail so it never showed while scrolling. Three-part fix: (1) CSS cascade reordering — `.rs-tab.is-active::before` (full tail spec: `background-color: inherit`, `border-left/bottom: 1px solid var(--color-black)`, `z-index: -1`, `height: 60px`) now declared AFTER `.rs-tab.is-stuck::before` so equal-specificity active rule wins; removed `.rs-tab.is-active.is-stuck::before`; added `.rs-tab-bar.is-stuck .rs-tab.is-active::before { height: 0 !important }` (bar-level collapse). (2) JS sentinel — new `initStickyBarDetection()` injects `.sticky-sentinel` divs before each `.rs-tab-bar` and uses IntersectionObserver to add `is-stuck` to the BAR when it physically docks; tab-level `is-stuck` preserved for global line `:has()` detection. (3) Global line fixes — `.rs-tab-bar::after` `left: -2.5vw` (was `0`) for left-edge bleed; `z-index: 5` (was `-1`) so line draws over inactive tabs (z-index:1) but under active tab (z-index:10). `.rs-tab` gets `z-index: 1`; `.rs-tab.is-active` gets `z-index: 10`. `overflow: visible !important` on `.rs-tab`.
**Next step:** Visual browser verification of tail showing while scrolling, collapsing when docked, and global line spanning full width.

### 2026-06-25 (v5)
**Task:** Tab color root-cause fix, design refinements — quotes, grid val, comparison images, carousel, text.
**Current state:** (1) Tab scrolling state rewritten: `.rs-tab-bar:not(.is-stuck) .rs-tab:not(.rs-tab-00)` now always gets accent fill + 80px + 60px tail + no name — independent of `.is-active`. Bypasses the bug where `.is-active` arrived late (only when section hit top third of viewport). (2) Docked state: `.rs-tab-bar.is-stuck .rs-tab` → black bg, accent text, border-color accent; `.rs-tab__name` shows; active+hover get accent fill. `::before` tail always collapses to 0 when stuck. (3) `.research-quote` → font-size 50px, padding-right calc(100%/12), no margin-inline:auto, no max-width. `.research-quote cite` → direction:rtl, text-align:right, same padding-right. (4) `.rs-section p` safety rule: padding-right/margin-right 0 !important. (5) `.rs-ms-grid-val` → padding-left: 30px !important. (6) `.img-wrapper` refactored: removed aspect-ratio:1/1, width 18vw→25vw, max-width 300→400px, height:auto; `.img-default` now relative+block; `.img-hover` absolute overlay. (7) Text: הטיפוס המזרחי location updated to "המזרח התיכון: ארץ ישראל, איראן, מצרים ולבנון". (8) Carousel buttons: click-to-skip replaced with press-to-accelerate via Web Animations API `playbackRate` (2.25x forward, -2.25x reverse while held).
**Next step:** Visual browser verification of tab colors, quotes, comparison grid images, and carousel acceleration.

### 2026-06-24 (v3)
**Task:** 5 surgical CSS edge-case refinements — header bleed, content right padding, tab hover, interaction text indent, sources viewer width.
**Current state:** (1) `.rs-section__hd` — removed `margin-inline-end: calc(-1 * var(--rs-page-margin))`. The negative margin was blocking the natural RTL left-overflow of the 96vw header in a 92vw grid, causing a 4vw black gap. Width stays `calc(100% + var(--rs-page-margin))`. (2) Grid column assignment rules for `.layout-text`, `.layout-media`, `.transition-text`, `.rs-beinoni-spectrum` all gained `padding-inline-start: 15px` — adds right breathing room without affecting the full-bleed header or sources. (3) Added `.rs-tab:hover { background-color: var(--tab-accent); color: var(--color-black); }` for top tabs. Updated `.bottom-tabs-rail .rs-tab.is-active` and `.bottom-tabs-rail .rs-tab:hover` to use `background-color: var(--tab-accent); color: var(--color-black)` (was text-only accent). (4) `.open-text-for-interaction` changed from `text-align: center` → `text-align: right; padding-inline-start: calc(100% / 3)` — indents one layout-text column from right. (5) `.rs-section__inner--sources` gained `width: calc(100% + var(--rs-page-margin))` to bleed into the body's left padding and reach viewport left edge.
**Next step:** Visual browser verification of all 5 fixes across sections.

### 2026-06-25 (v6)
**Task:** Sticky tab system — structural fix (rs-tab-00 extraction) + scroll-linked height shrink (80→30px).
**Current state:** Two structural problems fixed. (1) rs-tab-00 moved out of the first .rs-tab-bar into the outermost .rs-layer as an independent `<div class="rs-tab-00">` with `position: fixed; top: 0; right: calc(var(--rs-page-margin) + 1px); width: calc((100vw - 2*var(--rs-page-margin) - 1px) / 6); height: 30px; z-index: 350`. Every numbered section tab (rs-tab-0 through rs-tab-4) is now the sole tab in its own .rs-tab-bar. All `.rs-tab-bar .rs-tab.rs-tab-00` selectors replaced with standalone `.rs-tab-00` selectors; `:not(.rs-tab-00)` exceptions removed from scrolling-state rules. (2) `initTabTailSquish` + `initStickyBarDetection` merged into single `initStickyBars()`: one sentinel per bar shared by IntersectionObserver stuck-detection and rAF scroll-linked height. Height reads from `sentinel.getBoundingClientRect().top` (not from bar, which is clamped by sticky). Shrink zone: 60px. `--tab-height` (80→30px), `--tab-shrink-progress` (0→1), `--tab-tail-height` (60→0px) all set on each bar via rAF. `.rs-tab` uses `height: var(--tab-height, 80px)`; bar uses `margin-top: calc(-1 * var(--tab-height, 80px))`. `height: 80px !important` and is-stuck-context `height: 30px !important` removed from CSS. `.is-stuck` class kept for visual state only (colors, name visibility, global line position). Hero-state override `body.page-research:not(.is-past-hero) .rs-tab-bar { height: 30px !important; margin-top: -30px !important }` added to force 30px before hero passes.
**Next step:** Full browser verification at slow and fast scrolling speeds; confirm 00 מחקר fixed overlay aligns with col-1, height transition has no jump, global line fires correctly on dock.

### 2026-06-29
**Task:** Glyph-data JSON — complete OpenType feature restructure across all alternates subgroups.
**Current state:** `docs/data/glyph-data.json` rebuilt to accurately reflect shmuel-Regular.ttf GSUB feature table. Changes: (1) time category featureTag changed dlig→liga (clock glyphs are standard liga substitutions). (2) symbols category: ampersand.002 (non-existent) corrected to ampersand.001 (actual glyph name). (3) Entire alternates section restructured from 4 wrong subgroups to exactly 7 correct ones — liga, ss01, ss02, ss03, ss04, ss05, ss06 — with Hebrew labels per spec. Old wrong mappings fixed: wide Hebrew moved from ss04→ss01 using actual glyph names (uniFB21–uniFB28, hetWidehb); stylistic alternates moved from old ss03→ss06 using actual font names (uni05D0.001, uni05D8.001, uni05DC.001, uni05E1.001, uni05E7.001, uniFB24.001); ss03 now has circle forms; ss04 ring forms; ss05 square forms (new subgroup). liga subgroup expanded from 4 entries to 31 representative samples covering: Latin ligatures, Hebrew yod swash ligatures (uni05D105D9.swsh1 etc.), plus-triggered swashes, geresh/gereshayim punctuation swashes, contextual nun (nunheb001), text-to-symbol (No/and/tm/ש״ח), currency words, and clock sequences. ss02 fixed: removed non-existent zero.002, added ampersand.001. alef_lamed-hb (uni05D005DC) moved to ss06; yod-hb.001 (uni05D9.001) not exposed; all helper/private glyphs excluded. All 120 glyphNames in alternates verified against font.
**Next step:** Visual verification of the glyph-set UI — confirm all 7 subgroups render correctly in the browser; confirm inspector panel lookups work for swash and ligature entries.

### 2026-06-29 (v2)
**Task:** Glyph Set UI update — aligned specimen.js and specimen.css to the corrected 7-subgroup glyph-data.json.
**Current state:** specimen.js `buildCell` now adds `char-cell--multi` class and `data-display-len` attribute when `display.length > 1`. specimen.css: added `char-cell--ss05` and `char-cell--ss06` rules; removed stale `char-cell--dlig` (dlig no longer in JSON) and `char-cell--alt` (salt feature, never public); added `char-alt-group-label { white-space: normal; word-break: break-word }` for long Hebrew labels; added `char-cell--multi` rules with length-based font-size scaling and `overflow: hidden` to prevent unsubstituted multi-char trigger strings from spilling. `buildCharGrid` and `initCharInspector` unchanged — they already iterate JSON in order, apply featureTag and direction from JSON, and show '—' for null unicode/decimal.
**Next step:** Visual browser verification of all 7 alternates subgroups; test swash and ligature inspector lookups.

### 2026-06-29 (v3)
**Task:** Try Me page — OpenType feature controls content and wiring update.
**Current state:** try-me.html features section replaced: 7 old checkboxes (ss04/ss01/ss02/ss03/liga/dlig/frac with stale labels) replaced with 8 correct controls in spec order: liga (on by default), ss01, ss02, ss03, ss04, ss05, ss06, frac (all off by default). Old labels "אותיות רחבות" (ss04), "חלופות צורניות" (ss03) corrected. dlig removed. ss05 and ss06 added. English tooltip titles added to all feature labels. try-me.js `initOpenTypeFeatures` refactored: liga now always set explicitly (`"liga" 1` or `"liga" 0`) rather than relying on browser defaults; `reset()` method returned from function and wired into settings reset button (if present). Feature CSS output is now a fully explicit comma-separated list with no `normal` fallback when liga is enabled.
**Next step:** Visual browser verification — toggle each feature, verify reset restores defaults, check no console errors.
